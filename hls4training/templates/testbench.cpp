#include "project.h"
#include <stdio.h>
#include <math.h>

#define DEBUG true

int main () {

    // INITIALIZE INPUTS

    // INITIALIZE TRUTHS

    // DECLARE RESULTS

    // CALL PROJECT FUNCTION

    int count = 0;
    float error = 0;
    float val_real, val_pred;
    FILE* fout;

    // CALCULATE PERCENT ERROR

    return 0;

}